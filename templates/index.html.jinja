<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Time Series Forecasting</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        /* === Page Style === */
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #74ABE2, #5563DE);
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            color: #333;
        }

        .card {
            background-color: #fff;
            width: 480px;
            border-radius: 20px;
            box-shadow: 0 6px 25px rgba(0,0,0,0.15);
            padding: 40px 45px;
            text-align: center;
            transition: transform 0.2s ease-in-out;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        h1 {
            color: #4A4AD8;
            font-size: 1.8em;
            margin-bottom: 10px;
        }

        p {
            font-size: 0.95em;
            color: #555;
            margin-bottom: 20px;
        }

        input[type="text"] {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ccc;
            border-radius: 10px;
            font-size: 0.95em;
            outline: none;
            transition: all 0.3s ease;
        }

        input[type="text"]:focus {
            border-color: #4A4AD8;
            box-shadow: 0 0 5px rgba(74, 74, 216, 0.3);
        }

        button {
            background-color: #4A4AD8;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1em;
            transition: background 0.3s ease;
            width: 100%;
            margin-top: 15px;
        }

        button:hover {
            background-color: #3940b3;
        }

        .result {
            background-color: #F0F3FF;
            border-left: 5px solid #4A4AD8;
            padding: 15px;
            border-radius: 10px;
            margin-top: 25px;
            font-weight: 500;
            color: #333;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        }

        .error {
            color: #b30000;
            font-weight: 600;
            margin-top: 10px;
        }

        canvas {
            margin-top: 25px;
        }

        .footer {
            margin-top: 25px;
            font-size: 0.8em;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>🌤️ Temperature Forecasting</h1>
        <p>Enter the last <strong>10 temperature readings</strong> (comma-separated):</p>

        <form action="/predict" method="post">
            <input type="text" name="values" placeholder="25.3, 26.1, 27.0, ..." required>
            <button type="submit">Predict Next Temperature</button>
        </form>

        {% if error %}
            <div class="error">{{ error }}</div>
        {% endif %}

        {% if prediction %}
            <div class="result">
                Predicted next temperature: <strong>{{ prediction }}</strong>
            </div>

            <canvas id="forecastChart" width="400" height="200"></canvas>

            
            <script>
                const ctx = document.getElementById('forecastChart').getContext('2d');
                const chart = new Chart(ctx, {
                    type: 'line',
                    data: {
                        labels: {{ chart_labels|tojson }},
                        datasets: [{
                            label: 'Temperature (°C)',
                            data: {{ chart_values|tojson }},
                            borderColor: '#4A4AD8',
                            backgroundColor: 'rgba(74, 74, 216, 0.2)',
                            tension: 0.3,
                            fill: true,
                            pointRadius: 5,
                            pointBackgroundColor: '#4A4AD8'
                        }]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: { beginAtZero: false }
                        },
                        plugins: {
                            legend: { display: false },
                            title: {
                                display: true,
                                text: 'Past 10 + Next Forecast'
                            }
                        }
                    }
                });
            </script>
        {% endif %}

        <div class="footer">
            Powered by Flask · LSTM Deep Learning Model
        </div>
    </div>
</body>
</html>
